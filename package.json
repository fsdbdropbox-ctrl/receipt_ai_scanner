{
  "name": "receipt-ai-scanner-root",
  "version": "1.0.0",
  "description": "Root package.json for Railway deployment",
  "scripts": {
    "install": "cd backend && npm install",
    "start": "cd backend && npm start"
  },
  "engines": {
    "node": "18.x"
  }
}

